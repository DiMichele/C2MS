# ==============================================================================
# SUGECO - Environment Configuration
# ==============================================================================
# Copia questo file in .env e configura le variabili per il tuo ambiente.
# IMPORTANTE: Non committare mai il file .env nel repository!
# ==============================================================================

# ==============================================================================
# APPLICATION SETTINGS
# ==============================================================================
APP_NAME=SUGECO
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost/SUGECO/public
APP_TIMEZONE=Europe/Rome
APP_LOCALE=it
APP_FALLBACK_LOCALE=it
APP_FAKER_LOCALE=it_IT

# ==============================================================================
# SECURITY SETTINGS
# ==============================================================================

# Forza redirect a HTTPS (true in produzione)
# Se true, tutte le richieste HTTP verranno reindirizzate a HTTPS
FORCE_HTTPS=false

# Domini permessi per l'applicazione (separati da virgola)
# Lascia vuoto per permettere qualsiasi dominio (sconsigliato in produzione)
# Esempio: ALLOWED_HOSTS=sugeco.example.com,www.sugeco.example.com
ALLOWED_HOSTS=

# Session security settings
SESSION_SECURE_COOKIE=false
SESSION_HTTP_ONLY=true
SESSION_SAME_SITE=lax

# ==============================================================================
# DATABASE SETTINGS
# ==============================================================================
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=c2ms_db
DB_USERNAME=root
DB_PASSWORD=

# Database connection pooling (produzione)
# DB_POOL_MIN=2
# DB_POOL_MAX=10

# ==============================================================================
# CACHE & SESSION
# ==============================================================================
CACHE_DRIVER=file
SESSION_DRIVER=file
QUEUE_CONNECTION=sync

# Per produzione, considera:
# CACHE_DRIVER=redis
# SESSION_DRIVER=redis
# QUEUE_CONNECTION=redis

# ==============================================================================
# MAIL SETTINGS (per notifiche)
# ==============================================================================
MAIL_MAILER=smtp
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="noreply@sugeco.local"
MAIL_FROM_NAME="${APP_NAME}"

# ==============================================================================
# LOGGING
# ==============================================================================
LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

# Per produzione:
# LOG_LEVEL=warning

# ==============================================================================
# AUDIT LOG SETTINGS
# ==============================================================================

# Retention policy: giorni per cui mantenere i log nel database
# I log più vecchi vengono archiviati in CSV e poi eliminati
# Default: 365 giorni (1 anno)
AUDIT_RETENTION_DAYS=365

# Archivia i log in CSV prima di eliminarli (true/false)
# Se true, i log vengono esportati in storage/app/archives/audit-logs/
# prima di essere eliminati dal database
AUDIT_ARCHIVE_BEFORE_DELETE=true

# Directory per gli archivi (relativa a storage/app/)
AUDIT_ARCHIVE_DIRECTORY=archives/audit-logs

# Numero di log per pagina nell'interfaccia web
AUDIT_PER_PAGE=50

# ==============================================================================
# PRODUCTION CHECKLIST
# ==============================================================================
# Prima del deploy in produzione, assicurati di:
#
# 1. Impostare APP_ENV=production
# 2. Impostare APP_DEBUG=false
# 3. Generare una nuova APP_KEY: php artisan key:generate
# 4. Impostare FORCE_HTTPS=true
# 5. Configurare SESSION_SECURE_COOKIE=true
# 6. Configurare ALLOWED_HOSTS con i domini permessi
# 7. Configurare credenziali database sicure
# 8. Configurare un mail server reale
# 9. Considerare l'uso di Redis per cache/session
# 10. Impostare LOG_LEVEL=warning o error
# ==============================================================================
